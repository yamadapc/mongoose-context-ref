services: mongodb
language: node_js
node_js:
  - '0.10'
before_script:
    - npm install -g grunt-cli
script:
    - npm run ci
after_script:
    - ./node_modules/coveralls/bin/coveralls.js < lcov
